{
  model: // the output of go.Model.toJson(). Represents the layout and data stored, including the names given to the objects. 
          // Can be passed back in to the goDrawingService to load the model
  modelId: // the associated entity with this model. For example, if the abstract entity had an id of AE1, this would be AE1 
}